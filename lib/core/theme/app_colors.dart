// ─── App colors (light-mode palette) ─────────────────────────────────────────
// Centralised so they can be referenced consistently across this file.
import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF6F4F1);
  static const dark = Color(0xFF2D3748);
  static const medium = Color(0xFF4A5568);
  static const subtle = Color(0xFF718096);
  static const accentMint = Color(0xFFE8F5E9);
  static const mintBorder = Color(0xFFC6E6C8);
}
